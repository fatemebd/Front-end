import React from "react";
const Button=()=>{
    return(
        <button
            className="textarea-option bold"
            onClick={editStyles}
            value="bold"
          ></button>
    );
};